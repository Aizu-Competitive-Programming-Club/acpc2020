#include <iostream>
#include <vector>

using namespace std;

int main() {
  int N, M;
  cin >> N >> M;
  for ( int i = 0; i < N; i++ ) {
    double x, y;
    cin >> x >> y;    
  }
  for ( int j = 0; j < M; j++ ) {
    double x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;    
  }
  return 0;
}
